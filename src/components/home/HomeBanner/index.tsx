import React from 'react';
import {View} from 'react-native';

const HomeBanner = () => {
  return (
    <View
      style={{
        height: 200,
        backgroundColor: '#eeeeee',
      }}
    />
  );
};

export default HomeBanner;
